You are given an app.log file in the working directory.

Task:
- Create a file named errors.log
- It must contain ONLY the lines from app.log that contain the string "ERROR"
- Order must be preserved

This teaches basic log filtering and parsing essentials used in observability pipelines.
