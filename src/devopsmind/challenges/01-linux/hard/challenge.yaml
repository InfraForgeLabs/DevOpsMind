id: linux_hard_log_error_extract
title: "Extract Unique ERROR Logs (Hard)"
difficulty: hard
hint: "Use grep + awk + sort + uniq to extract timestamped ERROR messages."
validator: "validator.py"
inputs:
  - app.log
