<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>🏆 DevOpsMind Leaderboard</title>
  <style>
    body {
      font-family: "Segoe UI", Arial, sans-serif;
      background-color: #0d1117;
      color: #c9d1d9;
      text-align: center;
      padding: 40px;
    }
    h2 {
      margin-bottom: 20px;
    }
    table {
      margin: 0 auto;
      border-collapse: collapse;
      width: 80%;
      max-width: 800px;
      background-color: #161b22;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 0 10px #000;
    }
    th, td {
      padding: 12px 15px;
      border-bottom: 1px solid #30363d;
    }
    th {
      background-color: #21262d;
      color: #58a6ff;
    }
    tr:hover {
      background-color: #1c2128;
    }
    .rank {
      display: inline-block;
      padding: 3px 8px;
      border-radius: 6px;
      color: white;
      font-size: 0.85em;
    }
    .Beginner { background-color: #238636; }
    .Intermediate { background-color: #d29922; }
    .Advanced { background-color: #da3633; }
    .Expert { background-color: #8957e5; }
  </style>
</head>
<body>
  <h2>🏆 DevOpsMind Leaderboard</h2>
  <table id="board">
    <thead>
      <tr>
        <th>Name</th>
        <th>Username</th>
        <th>XP</th>
        <th>Rank</th>
        <th>Last Updated</th>
      </tr>
    </thead>
    <tbody></tbody>
  </table>

  <script>
    const url = "https://infraforgelabs.github.io/DevOpsMind/leaderboard/leaderboard.json";

    fetch(url)
      .then(res => {
        if (!res.ok) throw new Error("HTTP " + res.status);
        return res.json();
      })
      .then(data => {
        const tbody = document.querySelector("#board tbody");
        tbody.innerHTML = "";

        // Sort players by XP (highest first)
        data.players.sort((a, b) => b.xp - a.xp);

        data.players.forEach(entry => {
          const row = document.createElement("tr");
          row.innerHTML = `
            <td>${entry.gamer}</td>
            <td>${entry.username}</td>
            <td>${entry.xp}</td>
            <td><span class="rank ${entry.rank}">${entry.rank}</span></td>
            <td>${entry.timestamp}</td>
          `;
          tbody.appendChild(row);
        });
      })
      .catch(err => {
        document.querySelector("#board tbody").innerHTML = `
          <tr><td colspan="5" style="color:red;">Failed to load leaderboard.json (${err.message})</td></tr>
        `;
      });
  </script>
</body>
</html>
